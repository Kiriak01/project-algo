#include <iostream>
#include "includes/headers.hpp"
using namespace std; 


int main() 
{

    int j=0;
    for(i
        for(auto it = CV.begin(); it!= CV.end();++it){
            if(i==0 && p[i]!=*it){
                I.push_back(p[i]);
                j++;
            }else if(I[j] != p[i] && p[i] != *it){ 
                I.push_back(p[i]);
                j++;
            }    
        }


}